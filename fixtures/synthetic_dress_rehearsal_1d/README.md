# SYNTHETIC dress-rehearsal one-symbol daily pack

**These numbers are not market evidence.**

This pack exercises the first-cycle *shape* (one symbol, `1d`, `America/New_York`, `UNADJUSTED`, `max_staleness: NONE`) without vendor data. Every record is labeled `SYNTHETIC`. The symbol is `SYN:ONE`. It is not SPY and must not be treated as `HISTORICAL`, performance, edge, or a recommended trade.

`2024-01-02` through `2024-01-04` are invented session labels inside a later date window. They are not a claim that those dates were exchange sessions.

`manifest.json` is a file-integrity digest of this pack. It is not market evidence.
