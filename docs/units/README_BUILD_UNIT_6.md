# Radar V4 Build Unit 6 — Fixture Pack Loader

**Authorization:** `You can continue to build and not stop after building the units -TC`  
**Market method:** none  
**Network:** none

Load local `*.json` files from a directory. Only `FIXTURE` and `SYNTHETIC` envelopes may be accepted by this loader. `HISTORICAL` or `LIVE` labels are quarantined here even if identity-valid. This is not a market-data client.

Maximum claim: `BUILD UNIT 6 SOFTWARE/DATA FOUNDATION IMPLEMENTED WITHIN BOUNDED SCOPE`
