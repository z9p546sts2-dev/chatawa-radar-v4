# Radar V4 Build Unit 3 — JSON Document Intake

**Authorization:** `You can continue to build and not stop after building the units -TC`  
**Depends on:** accepted Build Units 1–2  
**Market method:** none

Parse a JSON object or array into envelopes, then reuse intake/quarantine. Unreadable JSON is not turned into a fake envelope.

Maximum claim: `BUILD UNIT 3 SOFTWARE/DATA FOUNDATION IMPLEMENTED WITHIN BOUNDED SCOPE`
